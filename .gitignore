# _site/ unignored so we can build locally and push for deploy (server serves _site, no Jekyll on server)
.sass-cache/
.jekyll-cache/
.jekyll-metadata
# Ignore folders generated by Bundler
.bundle/
vendor/

